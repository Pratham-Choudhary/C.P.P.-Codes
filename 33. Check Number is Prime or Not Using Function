#include <bits/stdc++.h>
using namespace std;

int prime(int a)
{
	if (a%2==0)
	{
		cout<<a<<" is Not Prime number";
	}
	else
	{
		cout<<"\n"<<a<<" is Prime Number";
	}
	return 0;
}

int main()
{
	int num,j;
	cout<<"Enter the number: ";
	cin>>num;
	j = prime(num);
	return 0;
}
