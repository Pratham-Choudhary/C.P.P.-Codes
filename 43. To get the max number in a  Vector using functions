#include <bits/stdc++.h>
using namespace std;

int max_number(vector<int>A)
{
	int max1 = INT_MIN;
	int j = 0;
	for(int i=0 ; i<A.size() ; i++)
	{
		max1 = max(max1,A[i]);
	}
	cout<<max1;
    return 0;
}

int main()
{
	vector<int>A={1,3,2,8,7,5};
	max_number(A);
	return 0;
}
