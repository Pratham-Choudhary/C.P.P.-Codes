#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n,reminder,reverse=0;
	cout<<"Enter the number to be reversed: ";
	cin>>n;
	while(n!=0)
	{
		reminder=n%10;
		reverse=reverse*10+reminder;
		n /=10;
	}
	cout<<reverse;
}
