#include <bits/stdc++.h>
using namespace std;

int add(int n);

int main()
{
	int n;
	cout<<"Enter the Number: ";
	cin>>n;
	cout<<"\nSum: "<<add(n);
	return 0;
}

int add(int n)
{
	if (n!=0)
	   return n + add(n-1);
	return 0;
}
