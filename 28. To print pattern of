#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n;
	cin>>n;
	for(int i=1; i<=n ; i++)
	{
		for(int j=1; j<=i; j++)
		{
			cout<<"*"; //for pattern you can change * anf for number just write "cout<<j;" this will print the number pattern
		}
	cout<<"\n";
	}
}
