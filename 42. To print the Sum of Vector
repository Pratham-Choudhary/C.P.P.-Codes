#include <bits/stdc++.h>
using namespace std;

int sum_vector(vector<int>A)
{
	int j=0;
	for (int i =0 ; i<A.size() ; i++)
	{
		j += A[i];
	}
	cout<<"SUM OF VECTOR A= "<<j;
	return 0;
}

int main()
{
	vector<int>A={1,3,6,4,2,9,10};
	sum_vector(A);
	return 0;
}
