#include <bits/stdc++.h>
using namespace std;

void print_revvector(vector<int>A)
{
	for(int i=8; i<A.size();--i)
	{
		cout<<" A[i]="<<A[i]<<" "<<endl;
	}
}

int main()
{
	vector<int>A={1,2,3,4,5,6,7,8,9};
	print_revvector(A);
}
